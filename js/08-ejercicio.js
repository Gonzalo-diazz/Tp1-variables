//8.- Escribe un programa que pida un número y diga si es divisible por 2

const n = parseInt(prompt("Ingrese un numero:"))

if(n % 2 === 0){
    alert(`El ${n} es divisible en 2`)
}
else{
     alert(`El ${n} no es divisible en 2`)
}
